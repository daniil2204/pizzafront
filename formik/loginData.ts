import { loginInterface } from "@/types";

export const initLogin:loginInterface = {
    email:'',
    password:'',
}

export const titleLoginObj:loginInterface = {
    email:"Email",
    password:"Пароль",
}


export const loginArray = ['email','password'];

